name: Test React App
on: push
jobs:
  runs-on: ubuntu-latest
  steps:
    - name: Get the repository code
      uses: actions/checkout@v3
    - name: Install deps
      run: npm ci
    - name: Test app
      run: npm run test
